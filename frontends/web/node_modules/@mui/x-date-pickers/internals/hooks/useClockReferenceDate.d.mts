import { MuiPickersAdapter, PickersTimezone, PickerValidDate } from "../../models/index.mjs";
import { PickerValue } from "../models/index.mjs";
export declare const useClockReferenceDate: <TProps extends {}>({
  value,
  referenceDate: referenceDateProp,
  adapter,
  props,
  timezone
}: {
  value: PickerValue;
  referenceDate: PickerValidDate | undefined;
  adapter: MuiPickersAdapter;
  props: TProps;
  timezone: PickersTimezone;
}) => PickerValidDate;