import { DefaultizedProps } from '@mui/x-internals/types';
import { ValidateDateProps } from "../validation/index.mjs";
import { PickerValidDate, TimezoneProps } from "../models/index.mjs";
export declare const useIsDateDisabled: ({
  shouldDisableDate,
  shouldDisableMonth,
  shouldDisableYear,
  minDate,
  maxDate,
  disableFuture,
  disablePast,
  timezone
}: ValidateDateProps & DefaultizedProps<TimezoneProps, "timezone">) => (day: PickerValidDate | null) => boolean;